import 'package:flutter/material.dart';

class regNow extends StatelessWidget {
  const regNow({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('Register'),
        ],
      ),
    );
  }
}
